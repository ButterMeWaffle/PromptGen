export class ExtraOptions {
    UseAll: boolean;
    RandomAmount: boolean;

    constructor (useAll: boolean, randomAmount: boolean){
        this.UseAll = useAll;
        this.RandomAmount = randomAmount
    }
}
